import { COLORS } from './colors';
import { theme, SPACING, FONTS, TEXT, SHADOWS, BORDER_RADIUS } from './theme';

export {
  COLORS,
  theme,
  SPACING,
  FONTS,
  TEXT,
  SHADOWS,
  BORDER_RADIUS
};

export default theme;